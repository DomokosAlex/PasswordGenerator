function red() {
    const overlay = document.getElementById('overlay');
    overlay.style.backgroundColor = 'rgba(255, 0, 0, 0.55)'; 
  }
  
  
  function blue() {
    const overlay = document.getElementById('overlay');
    overlay.style.backgroundColor = 'rgba(0, 0, 255, 0.55)'; 
  }
  
  
  function green() {
    const overlay = document.getElementById('overlay');
    overlay.style.backgroundColor = 'rgba(0, 255, 0, 0.55)'; 
  }